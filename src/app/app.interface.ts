export interface IOlympicData {
  symbol: string;
  priceChange: string;
  priceChangePercent: number;
  weightedAvgPrice: number;
  prevClosePrice: number;
  lastPrice: number;
  lastQty: number;
  bidPrice: number;
  askPrice: number;
  askQty: number;
}
